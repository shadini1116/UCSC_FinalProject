package tests;

import org.testng.Assert;
import org.testng.annotations.Test;
import pages.AppointmentPage;
import pages.CalendarPage;

/*public class CalendarTest extends BaseTest {

    @Test
    public void testNavigateToCalendarAndAddAppointment() {
        // Initialize AppointmentPage
        AppointmentPage appointmentPage = new AppointmentPage(driver);

        // Step 1: Navigate to the Calendar page
        CalendarPage calendarPage = appointmentPage.navigateToCalendarPage();
        Assert.assertTrue(calendarPage.areAppointmentsDisplayed(), "Appointments are not displayed for the current month!");

        // Step 2: Click "Add New Appointment" on the calendar page
        AppointmentPage newAppointmentPage = calendarPage.clickAddNewAppointment();

        // Step 3: Fill out the appointment form
        newAppointmentPage.fillAppointmentForm("2024-11-30", "2024-11-30", "Stevan", "11:00",   "Important discussion");

        // Step 4: Submit the form
        newAppointmentPage.submitAppointment();

        // Assert or print success message
        System.out.println("New appointment added successfully from the calendar page!");
    }
}*/

